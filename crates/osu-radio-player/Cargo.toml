[package]
name = "osu-radio-player"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
rodio = { version = "=0.22.2", default-features = false, features = ["playback", "mp3", "vorbis", "wav"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
